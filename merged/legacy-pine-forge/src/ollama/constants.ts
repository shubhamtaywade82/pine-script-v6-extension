export const OLLAMA_API_SECRET_KEY = 'pineForge.ollamaApiKey';

/** Default when `pineForge.ollama.model` is unset (must match `package.json` contributes default). */
export const DEFAULT_OLLAMA_MODEL = 'qwen3.5:4b';
